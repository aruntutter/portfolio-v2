const Divider = () => (
  <div
    className="h-px my-8 w-full"
    style={{
      background:
        "var(--gradient-divider, linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent))",
    }}
  />
);

export default Divider;
